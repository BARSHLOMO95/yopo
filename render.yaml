services:
  - type: web
    name: yopo-catalog
    env: static
    buildCommand: echo "No build needed - static HTML"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
